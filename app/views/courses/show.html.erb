<p id="notice"><%= notice %></p>
<h1><%= @course.year %> - <%= @course.name %></h1> 
<small>
	<%= link_to 'Edit', edit_course_path(@course) %> -- 
	<%= link_to "Change course", courses_path%> 
</small>

<br>
<button class="btn btn-primary"> <%= link_to 'Students', course_students_path(@course), {:style =>'color:white;'} %> </button>
<button class="btn btn-primary"> <%= link_to 'Evaluations', course_evaluations_path(@course), {:style =>'color:white;'} %> </button>

